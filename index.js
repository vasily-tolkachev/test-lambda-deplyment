exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Test checking out from git using pipline1'),
    };
    return response;
};
