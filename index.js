exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Test checking out from git using pipline'),
    };
    return response;
};
